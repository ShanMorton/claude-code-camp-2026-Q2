# Windows Zone.Identifier files (WSL download metadata)
*Zone.Identifier

# Environment / secrets
.env
.env.local

# Ruby / Bundler
vendor/
.bundle/
*.gem

# Boukensha runtime data
#.boukensha/sessions/

# Claude Code local settings (per-user permission overrides)
.claude/settings.local.json
